what happens when we try to insert null key or null value into ConcurrentHashMap
Throws NullPointerException - null value is not supported for key and value in ConcurrentHashMap

what if we have 2 interfaces with the same default method signatures, and a class implementing both interfaces, hw is the method implemented in the class without ambiguity



finalize method - poke details
jvm would be hanging around without ending the main thread, if we leave the executorservice without shutting it down.

OOM error, - errors are what kinds of exceptions - checked or unchecked? are they handled in the application, why or why not?
hardware failures come under errors?

errors are unchecked exceptions and we dont handle them in code. why? investigate


syntax of interface, default methods, other abstract methods how are they expected to look like?



hands on springboot -- thorough 
RestTemplate code, RestClient code to be practiced 

how we configure @Bean - custom beans and use them in the service layer, practice some examples to do hands on

exception handling code - remember the annotations - and syntax to demonstrate in coding round

Give some mock interviews for practice